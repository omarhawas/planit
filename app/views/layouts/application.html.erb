<!DOCTYPE html>
<html>
  <head>
    <title>Planit</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
  </head>

  <body>
    <div id="react-root"></div> <!-- Mount React here -->
    
    <!-- Rails notices/alerts (optional, if needed for non-React pages) -->
    <% if notice.present? %>
      <p class="notice"><%= notice %></p>
    <% end %>
    <% if alert.present? %>
      <p class="alert"><%= alert %></p>
    <% end %>
    
    <%= yield %> <!-- For Rails views content if needed -->
    
    <%= javascript_pack_tag "application", "data-turbo-track": "reload" %>
  </body>
</html>
